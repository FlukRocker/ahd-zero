// Floating sponsored ads. Hidden on mobile (<lg). Single close button hides
// all three at once for the rest of the session (sessionStorage flag).
//
// Slots:
//   left:   single banner pinned top-left (160x600 ish)
//   right:  single banner pinned top-right (160x600 ish)
//   bottom: array of 728x90 banners stacked bottom-center
//
// Each entry: { href, src, alt, rel? }. Set a slot to null or remove its key
// to hide that position.
{
    "left": {
    },
    "right": {
    },
    "bottom": [
    ]
}
